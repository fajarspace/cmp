baseURL : https://fajars.space/
languageCode : id
title : Fajar A Nugroho | @fajar.agngn

params:
    description: Menulis dengan ☕️

staticDir: static

disqusShortname: fajars-space

permalinks:
  blog : /:slug/
  rekomendasi : /:slug/
  tech : /:slug/

outputs:
    home : ["HTML", "RSS", "JSON"]
